{
  "name": "mcp-tool-definition",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "registerSearchTool: wraps a Vector DB search endpoint as an MCP tool for agent consumption",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run typecheck && pnpm run test",
    "example": "npx -y tsx examples/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
