{
  "name": "rate-limiter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "In-memory token bucket rate limiter (Pinecone mock interview)",
  "main": "src/rate-limiter.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
