{
  "name": "express-idempotency-middleware",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Express middleware that supports the Idempotency-Key header on write requests, scoped per-tenant, with verbatim response replay",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/express-serve-static-core": "^5.1.1",
    "@types/node": "^25.6.0",
    "@types/supertest": "^7.2.0",
    "express": "^5.1.0",
    "supertest": "^7.2.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
