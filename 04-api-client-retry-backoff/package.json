{
  "name": "api-client-retry-backoff",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "fetchWithRetry: a small fetch wrapper that adds retries with backoff",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
